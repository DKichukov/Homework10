import java.util.Arrays;import java.util.Scanner;public class upr2 {    public static void sumOddInArray() {        //declare variables and arrays        int[] numbers = {1, 25, 26, 4, 8, 13, 8, 75, 62, 3};        int count = 0;        //The sum of odd numbers in Array        System.out.print("The ODD numbers in array are: ");        for (int i = 0; i < numbers.length; i++) {            if(numbers[i]%2==0)            {                count += numbers[i];                System.out.print(numbers[i] + ", ");            }        }        //Print results;        System.out.println();        System.out.print("The sum of them is:");        System.out.print(count);    }    public static void averageOfAllPositive(){        //declare variables and arrays        int[] numbers = {1, 25, -26, 4, -8, 13, 8, 75, 62, 3};        float calc = 0f;        int count = 0;        // Calc the average of all positive numbers;        for (int i = 0; i < numbers.length; i++) {            if(numbers[i]>=0){                calc = (calc+numbers[i]);                count++;            }        }        //Print result        System.out.println("Calc: " + calc);        System.out.println("Total numbers of positive numbers: " + count);        System.out.println("The average is: " + calc/count);    }    public static void sortCopyArray(){        //declare variables        double randomNumber = 13;        //declare and initialize the scanner        Scanner sc = new Scanner(System.in);        System.out.println("Enter the size of your Array(enter number X): ");        int size = sc.nextInt();        //initialize the double Array        double[] doubleNumbers = new double[size];        for (int i = 0; i < size; i++) {            doubleNumbers[i] = randomNumber;            randomNumber+=20;            }        //sort the array        Arrays.sort(doubleNumbers);        double[] copy = new double[size];        for (int i = 0; i < size; i++) {            copy[i] = doubleNumbers[i];        }        for(double number:copy){            System.out.println(number);        }    }    public static void copyNumberInArray(){        //declare array        int[] numbers = {1, 25, -26, 4, -8, 13, 8, 75, 62, 3};        //copy array number        int[] newArray = new int[10];        for (int i = 0; i < numbers.length; i++) {            newArray[i] = numbers[i];        }        System.out.println("New array elements: ");        for(int number:newArray)        System.out.print(number + " ");    }    public static void main(String[] args) {       // sumOddInArray();        System.out.println("-----------------------------");        //averageOfAllPositive();        //sortCopyArray();        copyNumberInArray();    }}